
<div id="follow_form_<%= user.id %>">
  <% if user.id != current_user.id %>
    <% unless current_user.following?(user) %>
      <%= form_for(current_user.relationships.build(followed_id: user.id), remote: true) do |f| %>
        <%= f.hidden_field :followed_id %>
        <%= f.submit "フォロー", class: "btn btn-large btn-primary", style: "margin-bottom: 1rem"%>
      <% end %>
    <% else %>
      <%= form_for(current_user.relationships.find_by(followed_id: user.id), html: { method: :delete }, remote: true) do |f| %>
        <%= f.submit "つながりを解除", class: "btn btn-large", style: "pmargin-bottom: 1rem" %>
      <% end %>
    <% end %>
  <% end %>
</div>
